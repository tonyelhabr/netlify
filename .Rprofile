
library("base")
library("methods")
library("datasets")
library("utils")
library("grDevices")
library("graphics")
library("stats")

path_r_profile <- "~/.Rprofile"
if(file.exists(path_r_profile)) {
  source(path_r_profile)
}
rm("path_r_profile")


# Reference: https://bookdown.org/yihui/blogdown/global-options.html.
# if (file.exists("~/.Rprofile")) {
#   base::sys.source("~/.Rprofile", envir = environment())
# }
# options(servr.daemon = TRUE)
options(servr.daemon = FALSE)
library("blogdown")
